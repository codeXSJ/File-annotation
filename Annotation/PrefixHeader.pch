//
//  PrefixHeader.pch
//  Annotation
//
//  Created by 许公子 on 2020/7/23.
//  Copyright © 2020 ppt. All rights reserved.
//

#ifndef PrefixHeader_pch
#define PrefixHeader_pch
#import <UIKit/UIKit.h>
#import <Masonry.h>
#import "Methods.h"
#import "MBProgressHUD+MJ.h"
#import "MBProgressHUD+ShowInWindow.h"
#import "JXTAlertView.h"
#import "UIColor+Contract.h"
//以下适配，只做提示用，实际使用，如果要适配iOS7，对应方法还是需要自行适配
//不然的话，可能因为这个宏，导致bug，因为新版xcode编译，对应方法是可以使用的（因为xcode的API存在），但是实际在对应系统上，可能就崩溃了
#ifdef NSFoundationVersionNumber_iOS_8_0
    #import "JXTAlertController.h"
#endif

#define NSStringFormat_s(s)   [NSString stringWithFormat:@"%@",(s)]
#define NSStringFormat_d(d)   [NSString stringWithFormat:@"%d",(d)]

//颜色
#define MainBlueColor          ColorFromRGB(0x5084FC)
#define JDC_MianColorBule      JDC_UIColorHex(#3F8EF7)
#define JDC_WhiteColor         JDC_UIColorHex(#FFFFFF)
#define SignatureColorDefault  ColorFromRGB(0x000000)
#define ColorFromRGB(rgbValue) [UIColor colorWithRed:((float)((rgbValue & 0xFF0000) >> 16))/255.0 green:((float)((rgbValue & 0xFF00) >> 8))/255.0  blue:((float)(rgbValue & 0xFF))/255.0 alpha:1.0]


//尺寸
#define ScreenWidth         [[UIScreen mainScreen] bounds].size.width
#define ScreenHeight        [[UIScreen mainScreen] bounds].size.height
#define isFullScreenC       (ScreenHeight >= 812.0)
#define NaviHeight          (isFullScreenC ? 88 : 64)
#define TabbarHeight        (isFullScreenC ? 83 : 49)
#define BottomHeight        (isFullScreenC ? 34 : 0)
#define StatusHeight        (isFullScreenC ? 44 : 20)
#define SealSize            80
#define viewSize            24
#define view_X              viewSize/2
#define delBtnSize          18
#define moveViewSize        24


//tag初始值

#define Text_Tag            10000
#define DragView_Tag        20000
#define DelButton_Tag       30000


//弱引用
#define WeakSelf(type)    __weak typeof(type) weak##type = type;
#define StrongSelf(type)  __strong typeof(type) type = weak##type;

//单例宏
#undef    AS_SINGLETON
#define   AS_SINGLETON( ... ) \
+ (instancetype)sharedInstance;

#undef    DEF_SINGLETON
#define   DEF_SINGLETON( ... ) \
+ (instancetype)sharedInstance{ \
static dispatch_once_t once; \
static id __singleton__; \
dispatch_once( &once, ^{ __singleton__ = [[self alloc] init]; } ); \
return __singleton__; \
}

// Include any system framework and library headers here that should be included in all compilation units.
// You will also need to set the Prefix Header build setting of one or more of your targets to reference this file.

#endif /* PrefixHeader_pch */
